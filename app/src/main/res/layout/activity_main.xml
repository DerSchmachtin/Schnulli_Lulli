<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="20dp"
    android:background="?android:attr/windowBackground">

    <!-- Welcome Text -->
    <TextView
        android:id="@+id/welcome_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Hallo Lulli!"
        android:textAppearance="@style/TextAppearance.Material3.TitleLarge"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="8dp"/>

    <!-- Day Counter -->
    <TextView
        android:id="@+id/day_counter"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="0 tolle Tage mit dir ❤️"
        android:textAppearance="@style/TextAppearance.Material3.BodyLarge"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="16dp"/>

    <!-- Sync Button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/sync_button_main"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="🔄 Aktualisieren"
        android:textSize="14sp"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="16dp"
        style="@style/Widget.Material3.Button.TonalButton"/>

    <!-- Daily Message Card with Flip Animation -->
    <FrameLayout
        android:id="@+id/daily_message_container"
        android:layout_width="match_parent"
        android:layout_height="280dp"
        android:layout_marginBottom="24dp"
        android:layout_marginTop="20dp"
        android:clipChildren="false"
        android:clipToPadding="false">

        <!-- Card Front (Hidden Message) -->
        <com.google.android.material.card.MaterialCardView
            android:id="@+id/card_front"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:layout_gravity="center"
            app:cardCornerRadius="24dp"
            app:cardElevation="8dp"
            app:strokeWidth="2dp"
            android:clickable="true"
            android:focusable="true">

            <include layout="@layout/card_front" />

        </com.google.android.material.card.MaterialCardView>

        <!-- Card Back (Revealed Message) -->
        <com.google.android.material.card.MaterialCardView
            android:id="@+id/card_back"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:layout_gravity="center"
            app:cardCornerRadius="24dp"
            app:cardElevation="8dp"
            app:strokeWidth="2dp"
            android:visibility="gone"
            android:clickable="true"
            android:focusable="true">

            <include layout="@layout/card_back" />

        </com.google.android.material.card.MaterialCardView>

    </FrameLayout>

    <!-- Timeline Card -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/timeline_card"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:cardCornerRadius="20dp"
        app:cardElevation="6dp"
        app:strokeColor="?attr/colorSecondary"
        app:strokeWidth="1dp">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/view_timeline_button"
            style="@style/Widget.LoveApp.Button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="📅 Timeline ansehen"
            android:textSize="18sp"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            android:layout_margin="10dp"/>
    </com.google.android.material.card.MaterialCardView>


</LinearLayout>
